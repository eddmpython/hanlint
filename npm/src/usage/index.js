// @ts-check
/**
 * 용례 (usage). 실제 글에서 낱말이 어떻게 쓰였는지를 든다. 뜻은 파이썬 usage/ 가 소유한다.
 * fingerprint 와 같은 층이다. rules 가 쓰고, analysis 와 config 와 data 만 쓴다.
 */
export { attested, chainDocuments, usageKindOf, usageTable } from "./counts.js";
