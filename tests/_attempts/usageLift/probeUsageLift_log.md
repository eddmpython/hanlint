# usageLift 로그

## 요약 (2026-09-19 기준)

**용례를 프롬프트에 붙이면 고침이 나아진다는 증거는 없다.** 300쌍을 두 모델과 세 조건으로 재고 채점을 말뭉치의 다른
절반으로 한 실험에서, 돌리기 전에 못박은 조건 셋을 하나도 넘지 못했다. 3회차 첫 채점에 버그가 있어 (교집합 캡) 다시
쟀고, 정확히 세니 차이가 오히려 더 줄었다 (강한 모델 평균 차 +0.040 → +0.009, p 0.0172 → 1.0000).

**AI 글과 사람 글은 낯선 결합 비율로 갈리지 않는다.** 사람이 쓴 사업보고서 문장이 22.1%, AI 가 고친 문장이 26~27% 다.
"낯선 결합을 세어 AI 글을 가려내자" 는 방향도 이 수로 접는다.

용례 엔진의 확인된 값은 규칙 쪽 지적 감소 (`scripts/measure/reports.py`, 1,000문장당 667.6 → 371.8) 뿐이다.
1, 2회차의 45쌍 수치 (6 대 1, 1 대 6) 는 인용하지 않는다.

## 3회차 사전 등록 (2026-09-19, 돌리기 전에 적는다)

1, 2회차가 증명이 못 된 까닭은 둘이다. 표본 45쌍은 방향을 말하기에 작았고, 지표가 순환이었다 (프롬프트로 준 용례의
낱말을 썼는지를 같은 말뭉치로 채점했다). 셋째 판은 그 둘을 고치고 **판정 조건을 돌리기 전에** 못박는다.

**설계.** 사업보고서 3,193편을 문서 단위로 반으로 가른다 (짝수 번째 A, 홀수 번째 B). 근거는 A 색인에서 주고 채점은
B 색인으로 한다. 과제 문장은 A 문서에서 고른다. 규칙마다 100문장, 모두 300쌍. 조건 셋 (reasonOnly, withUsage, withWords).

**주 지표 genreHit.** 고친 문장의 이웃 어절 쌍 가운데 원문에 없던 새 쌍만 골라, 그것이 B 색인의 문서 둘 이상에 나오는
비율. A 에서 본 결합을 그대로 베껴도 B 에 없으면 점수가 안 된다.

**판정 조건 (셋 다 넘어야 효과 있음으로 본다).**

1. 짝 부호 검정 p < 0.01 (같은 문장, 같은 모델, 조건만 다름)
2. genreHit 평균 차이 ≥ +0.05
3. 새 error 가 늘지 않음 (조건 합계 기준)

하나라도 못 넘으면 **효과 없음** 으로 적고 문서 어디에도 효과를 쓰지 않는다. 표본과 지표를 결과를 본 뒤에 바꾸지 않는다.

**한계 (미리 적는다).** genreHit 는 그 종류의 실제 글에 있는 결합인지를 묻지 그 문장이 좋은지를 묻지 않는다. 뜻이
바뀌었는지도 묻지 않는다 (그건 사람이 읽어야 한다). 새 error 와 규칙 해결이 그 둘을 부분적으로만 지킨다.

## 1회차 (2026-09-19 오전). 용례 유무 두 조건, 45쌍

대상 커밋 7548c32. 사업보고서 말뭉치 앞 200편에서 report 프리셋의 지적 가운데 nounPile, euiChain, translationese 를
규칙마다 15문장, 문장 해시 순으로 골랐다. 조건 둘. `reasonOnly` 는 규칙과 이유만, `withUsage` 는 거기에 `hanlint usage`
가 낸 문장 다섯을 더 받았다. manifest SHA256 `27767bf6655387cc3e31e132be519f28d8fbc48822bfee1d5dde808961e11620`.

채점은 결정적이다. 규칙 해결 (고친 문장에서 목표 규칙이 사라짐), 새 error, 명사 보존 (원문의 명사 어절이 고친 문장에
남은 비율), 길이 비.

### qwen3:8b (Ollama 0.23.2, digest 500a1f06…, temperature 0, seed 42, think false)

응답 SHA256 `031b0b0d46384a08d2bc206cb601f370f60492ea7c9934429c48434af3d27a53`, 90응답 누적 199.5초.

```text
             규칙 해결  원문 그대로  새 error  명사 보존
reasonOnly     19/45        9          0        0.98
withUsage      21/45        3          2        0.98

짝별: 용례만 해결 7, 이유만 해결 5, 같은 결과 33. 명사 보존 높음 5, 낮음 7, 같음 33
```

### claude-opus-5 fresh-context 서브에이전트 (조건마다 45과제를 한 묶음으로)

응답 SHA256 `824e83ed5f9198a349cacc08b7b6c2a563a096b0ce0f85ded79b501086bc2d7b`.

```text
             규칙 해결  새 error  명사 보존
reasonOnly     44/45       1        0.94
withUsage      44/45       2        0.96

짝별: 해결 0 대 0 (같음 45). 명사 보존 높음 6, 낮음 1, 같음 38
```

그때 읽은 것. 규칙 해결은 용례 없이도 천장이고 달라진 것은 낱말이다. 용례가 있으면 `수취`, `투자금액`, `영역별` 을
그대로 두고 없으면 `받을`, `투자한 금액`, `영역마다` 로 바꿨다. **이 관찰은 2회차에서 재현되지 않았다.**

## 2회차 (같은 날 오후). 조건 셋, 새 표본 45쌍

대상 커밋 21dffbe. 조건에 `withWords` 를 더했다 (문장 다섯에 더해 함께 쓰는 말: 낱말마다 뒤 용언, 뒤 명사, 앞 명사와
각각의 문서 수). 표본이 1회차와 다른 까닭은 그 사이 규칙이 바뀌어서다. 빈도표가 961편에서 3,193편으로 늘어 nounPile 이
접는 연쇄가 늘었고, 사전 규칙이 관용 항목을 넘기게 되어 translationese 지적이 크게 줄었다. 같은 조건으로 다시 뽑으니
다른 문장이 걸렸다. manifest SHA256 `fefb1727202b0be161c8cca1e05149a9f3c4fe0f62fd20b1868151463d1b838f`
(앞 200편, 규칙마다 15). 응답 SHA256
은 qwen3:8b `42eabf0ba67a7f414996ad5109d705362bbe1ce784edecd27908b02375b7f22b`, 에이전트
`1598e438913920b5919d92dbd6c8a7c7e7b2c23f3f17942a1b5d58eb69a3c390` 다.

### qwen3:8b (같은 설정)

```text
             규칙 해결  원문 그대로  새 error  명사 보존
reasonOnly     19/45        9          0        0.98
withUsage      21/45        3          2        0.98
withWords      17/45        8          1        0.97

짝별 (reasonOnly 대): withUsage 해결 7 대 5, 명사 보존 높음 5 낮음 7
                       withWords 해결 6 대 8, 명사 보존 높음 5 낮음 10
```

### claude-opus-5 fresh-context 서브에이전트 (조건마다 한 묶음)

```text
             규칙 해결  새 error  명사 보존
reasonOnly     43/45       0        0.96
withUsage      43/45       2        0.95
withWords      42/45       2        0.96

짝별 (reasonOnly 대): withUsage 해결 0 대 0, 명사 보존 높음 1 낮음 6
                       withWords 해결 0 대 1, 명사 보존 높음 2 낮음 2
```

## 판정

1회차에서 용례 쪽이 지켰다고 본 바로 그 문장 (`고객과의 계약에서 … 수취가 예상되는 대가`) 이 2회차에서는 반대로
움직였다. 1회차는 용례 조건이 `수취` 를 지키고 이유 조건이 `받을` 로 바꿨는데, 2회차는 이유 조건이 `수취` 를 지키고
용례 조건이 `받을` 로 바꿨다. 조건이 아니라 표본과 호출이 흔든 것이다.

- **규칙 해결**: 강한 모델은 어느 조건이든 천장 (42~44/45). 작은 모델은 조건 사이에서 진동한다 (17~21/45).
- **명사 보존**: 방향이 회차마다 뒤집힌다. 효과 없음으로 본다.
- **함께 쓰는 말 (withWords)**: 문장만 주는 것보다 나은 자리를 못 찾았다. 작은 모델에서는 오히려 해결이 줄었다
  (19 → 17). 프롬프트가 길어져 지시를 밀어낸 것으로 보이나 확인하지 않았다.

그래서 문서 어디에도 "용례가 고침을 낫게 한다" 고 적지 않는다. 용례 엔진은 **묻는 자리에 사실을 대는 것** 으로만
설명한다. 효과를 재려면 표본을 45가 아니라 수백으로 키우고 같은 표본을 조건마다 여러 번 호출해 호출 잡음을
분리해야 한다. 그 전에는 수치를 인용하지 않는다.

측정 자체의 한계도 남긴다. `명사 보존` 은 원문 명사 어절이 고친 문장에 글자 그대로 남았는지만 센다. `수취` → `받을`
처럼 뜻이 같고 낱말이 바뀐 것을 잃음으로 세는데, 그것이 나쁜 고침인지는 이 지표가 답하지 못한다. 사람이 읽어야 한다.

## 3회차 결과 (2026-09-19 저녁). 300쌍, 조건 셋, 모델 둘

대상 커밋 9f8e97a. 사업보고서 3,193편을 문서 단위로 반으로 갈라 A (1,597편) 를 근거 색인으로, B (1,596편) 를 채점
말뭉치로 썼다. 과제는 A 문서에서 규칙마다 100문장, 문서 262편에서 나온 300쌍이다.
manifest SHA256 `2745c2a8542197d6c82c5350…` (첫 채점과 같은 과제다. 달라진 것은 채점뿐이다).

### 첫 채점의 버그와 고침

첫 채점은 쌍 하나를 확인할 때 두 낱말이 함께 든 문장 400개만 읽고 끊었다 (PAIR_CHECK). `금융 관련` 처럼 두 낱말이
각각 10만 편에 나오는 흔한 결합은 교집합이 14,386문장인데 그 앞 400개에 붙어 있는 자리가 없어 **없다고 세었다.**
흔한 낱말일수록 틀렸다. 고친 방식은 표본도 캡도 쓰지 않는다. 채점에 필요한 쌍 (6,824종) 을 모두 모은 뒤 채점 말뭉치
1,596편을 한 번 훑어 문서 수를 정확히 센다 (약 2분).

고친 채점이 결론을 바꾸지 않았다. 오히려 효과가 더 작아졌다.

```text
강한 모델 withUsage 대 reasonOnly
                 높음/낮음      평균 차    부호검정 p
버그 있던 채점    84 / 55       +0.040      0.0172
정확한 채점       65 / 64       +0.009      1.0000
```

### claude-opus-5 fresh-context 서브에이전트 (100과제 묶음 아홉)

응답 SHA256 `f39775706cf6dadee206f80a…`.

```text
             규칙 해결   새 error  genreHit 평균  낯선 결합   명사 보존
사람 원문         -          -           -        22.1%         -
reasonOnly    295/300       2         0.294       27.5%       0.950
withUsage     299/300      10         0.311       26.9%       0.962
withWords     298/300       6         0.309       26.1%       0.962

짝 비교 (reasonOnly 대)
withUsage  genreHit 높음 65 낮음 64 같음 139 (짝 268), 평균 차 +0.009, p 1.0000, 새 error +8
withWords  genreHit 높음 61 낮음 59 같음 154 (짝 274), 평균 차 +0.009, p 0.9273, 새 error +4
```

### qwen3:8b (Ollama 0.23.2, digest 500a1f067a9f7826…, temperature 0, seed 42, think false)

응답 SHA256 `c3c42ab589ad7351bff3f12b…`, 900응답 누적 1,917초.

```text
             규칙 해결   새 error  genreHit 평균  낯선 결합   명사 보존
reasonOnly    105/300       8         0.393       22.4%       0.965
withUsage      97/300       5         0.432       22.4%       0.950
withWords      99/300       5         0.425       22.8%       0.949

짝 비교 (reasonOnly 대)
withUsage  genreHit 높음 40 낮음 39 같음 90 (짝 169), 평균 차 +0.006, p 1.0000, 새 error -3
withWords  genreHit 높음 43 낮음 39 같음 84 (짝 166), 평균 차 +0.028, p 0.7407, 새 error -3
```

### 사전 등록 조건에 대조

| 조건 | 강한 withUsage | 강한 withWords | 작은 withUsage | 작은 withWords |
|---|---|---|---|---|
| 부호검정 p < 0.01 | 1.0000 미달 | 0.9273 미달 | 1.0000 미달 | 0.7407 미달 |
| genreHit 차 ≥ +0.05 | +0.009 미달 | +0.009 미달 | +0.006 미달 | +0.028 미달 |
| 새 error 안 늘어남 | +8 미달 | +4 미달 | -3 통과 | -3 통과 |

**넷 다 효과 없음으로 판정한다.**

### 딸린 실측: 낯선 결합으로는 AI 글을 못 가려낸다

용례를 검사 쪽으로 옮기는 방법을 보려고 같은 실행에서 하나 더 쟀다. 문장 안에서 두 낱말이 각각 문서 100편 이상에
나오는데 그 결합은 채점 말뭉치 두 편에도 없는 자리의 비율이다.

```text
사람이 쓴 사업보고서 문장   22.1% (717/3,248)
AI 고침 (용례 없이)        27.5% / 22.4%   (강한 모델 / 작은 모델)
AI 고침 (용례)             26.9% / 22.4%
```

사람 글도 22%가 걸린다. 사람과 AI 의 차이는 5%p 안쪽이고 작은 모델에서는 아예 같다. **낯선 결합 비율은 AI 글을 가려내는
신호가 아니다.** 문장마다 잡는 규칙으로도 못 쓴다 (사람 문장의 11.3%가 걸린다. 낱말 문턱 100편 기준).

### 남는 판단

- 프롬프트에 무엇을 얹는 방식은 네 번 재서 네 번 다 효과가 없었다. 이 방향은 닫는다.
- 낯선 결합을 세는 방식도 위 수로 닫는다.
- 아직 안 해 본 것은 용례를 `fix` 와 `candidates` 로 넣는 것이다. 프롬프트가 아니라 제품 통로이고, 이 실험이 계속
  보여 준 문제 (명사 쌓기를 고치다 `의` 사슬을 만들어 새 error 가 2건에서 10건으로) 를 직접 겨눈다. 하려면 말뭉치에서
  꺼낸 고침 후보가 실제로 규칙을 통과하는지부터 재야 한다.
